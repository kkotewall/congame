var keyLetters = [];

function Word() {

	// function to split keyWord into letters
	function wordSplitter() {
	  keyLetters = chosenWord.split(",");
	  // We count the number of letters in the word.
	  placeHolder = keyLetters.length;
	 };

};